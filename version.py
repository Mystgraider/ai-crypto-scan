# Centralized project version metadata
#
# Keep these values aligned with config.py. This module is currently not
# imported by production code, but matching the active configuration avoids
# stale metadata for tools or future callers that use version.py.
DB_VERSION       = "1.0.0"
CONFIG_VERSION   = "2.2.0"
STRATEGY_VERSION = "6.2.0"
